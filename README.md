# OpenCore-CometLake-EFI
Base OpenCore EFI files for Intel Comet lake cpus(10XXX)
Everything (OC EFI files and kexts) are debug builds
Change everything to release build after successful install

-----------------------------------------------------------------

Boots up perfectly fine on my device
